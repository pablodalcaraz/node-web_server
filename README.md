# Servidor web con NodeJS
Realizamos un servidor que sirve a una red social X
## Como usarlo?
1. Debes hacer git clone de `https://github.com/pablodalcaraz/node-web_server`
2. Abrir en tu editor de código y hacer `npm install`	
3. Crea tu variable de entorno `.env`con la plantilla que dejamos disponible. Recomendado(PORT:3000 y carpeta 'PUBLIC')
4. Ejecuta tu proyecto con `npm start`